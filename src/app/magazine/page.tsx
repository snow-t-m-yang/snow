import CreateCardModal from "./_components/CreateCardModal";
import MagaCard from "./_components/MagaCard";

type Props = {}
export default function page({}: Props) {
  return (
    <main className=''>
      <MagaCard />
    </main>
  );
}